---
/* The anatomy diagram is an Annotated frame, so this page needs its sheet. */
import "@skryensya/core/components/annotation.css";
import { annotationDemoCss } from "../../demos/annotation";
import { inlineAnatomyTree } from "../../demos/layout-anatomy";
import ComponentPageShell from "./ComponentPageShell.astro";
import CodeBlock from "../CodeBlock.astro";
import ComponentPreview from "../ComponentPreview.astro";
import { inlineCardFloorTree, inlineTree } from "../../demos/layout";
import { getLocale, useTranslations } from "../../i18n";

const install = `pnpm add @skryensya/core @skryensya/react`;
const t = useTranslations(getLocale(Astro.url));
---

<ComponentPageShell
  title="Inline"
  description={t("inlinePage.description")}
  contractId="layout"
  tests={[
    {
      file: "packages/react/src/components/layout.test.tsx",
      tests: [
        {
          name: "renders Stack, Inline and Grid as the documented layout contracts",
          description: t("inlinePage.test1"),
        },
        {
          name: "omits data-block-start when Inline's space above is the default none",
          description: t("inlinePage.test2"),
        },
      ],
    },
  ]}
>
  <Fragment slot="usage">
    <p class="lede" set:html={t("inlinePage.lede")} />
    <ComponentPreview tree={inlineTree(t)} label={t("inlinePage.previewLabel")} note={t("inlinePage.previewNote")} />

    <p set:html={t("inlinePage.anatomyBody")} />
    <ComponentPreview
      tree={inlineAnatomyTree(t)}
      css={annotationDemoCss}
      label={t("inlinePage.anatomyPreviewLabel")}
      screens={false}
      hideSource
      chrome={false}
    />

    <h3>{t("inlinePage.floorTitle")}</h3>
    <p set:html={t("inlinePage.floorBody")} />
    <ComponentPreview tree={inlineCardFloorTree(t)} label={t("inlinePage.floorPreviewLabel")} note={t("inlinePage.floorPreviewNote")} />

    <h3>{t("inlinePage.whereTitle")}</h3>
    <p set:html={t("inlinePage.whereBody")} />

    <h3>{t("inlinePage.htmlTitle")}</h3>
    <p set:html={t("inlinePage.htmlBody")} />

    <p set:html={t("component.reactNote")} />

    <h3>{t("component.contractNotesTitle")}</h3>
    <ul>
      <li set:html={t("inlinePage.contractItem1")} />
      <li set:html={t("inlinePage.contractItem2")} />
      <li set:html={t("inlinePage.contractItem3")} />
      <li set:html={t("inlinePage.contractItem4")} />
      <li set:html={t("inlinePage.contractItem5")} />
      <li set:html={t("inlinePage.contractItem6")} />
    </ul>
  </Fragment>

  <Fragment slot="install">
    <CodeBlock code={install} label="terminal" />
  </Fragment>
</ComponentPageShell>
