---
/* The CSS block below documents this import; the page never actually made it. Without the base
   `.sk-placeholder` rule nothing sets `block-size`, so every text-shaped line measured 0px and the
   skeleton painted only its media block: the one thing this page exists to show. */
import "@skryensya/core/components/placeholder.css";
import "../../examples/placeholder.css";
import ComponentPageShell from "./ComponentPageShell.astro";
import CodeBlock from "../CodeBlock.astro";
import ComponentPreview from "../ComponentPreview.astro";
import {
  placeholderMatchTree,
  placeholderVocabularyTree,
  publicationPlaceholderTree,
  publicationSwapScript,
  publicationSwapTree,
} from "../../demos/placeholder";
import { getLocale, useTranslations } from "../../i18n";
import contractSource from "@contracts/semantic/placeholder.yaml?raw";
import coreSource from "@core/src/placeholder.ts?raw";
import cssSource from "@core/css/components/placeholder.css?raw";
import reactSource from "@react/src/components/placeholder.tsx?raw";
import reactTestSource from "@react/src/components/placeholder.test.tsx?raw";

const install = `pnpm add @skryensya/core @skryensya/react`;
const t = useTranslations(getLocale(Astro.url));
const swapScript = publicationSwapScript;
const styles = `@import "@skryensya/core/components/placeholder.css";
@import "@skryensya/core/patterns/layout.css";
@import "@skryensya/core/components/badge.css";
@import "@skryensya/core/components/avatar.css";
@import "@skryensya/core/components/typography.css";`;

const sourceFiles = [
  { id: "contracts/semantic/placeholder.yaml", source: contractSource, language: "yaml" },
  { id: "packages/core/src/placeholder.ts", source: coreSource, language: "ts" },
  { id: "packages/core/css/components/placeholder.css", source: cssSource, language: "css" },
  { id: "packages/react/src/components/placeholder.tsx", source: reactSource, language: "tsx" },
  { id: "packages/react/src/components/placeholder.test.tsx", source: reactTestSource, language: "tsx" },
] as const;
---

<ComponentPageShell
  title="Placeholder"
  description={t("placeholderPage.description")}
  contractId="placeholder"
  sourceFiles={sourceFiles}
  styleGroups={[{ file: "components/placeholder.css", probe: "sk-placeholder" }]}
  tests={[
    {
      file: "packages/react/src/components/placeholder.test.tsx",
      tests: [
        {
          name: "sizes a line by the type role it replaces, not by a hand-written length",
          description: t("placeholderPage.test1"),
        },
        {
          name: "draws a paragraph as real lines, clamped, with the raggedness authored on the root",
          description: t("placeholderPage.test3"),
        },
        {
          name: "clamps the rendered line count the same way the emitter does",
          description: t("placeholderPage.test4"),
        },
        {
          name: "gives a filled block its parent's whole box, and a circle the avatar scale",
          description: t("placeholderPage.test5"),
        },
        {
          name: "keeps every signature out of the accessibility tree",
          description: t("placeholderPage.test6"),
        },
        {
          name: "has no serious accessibility violations inside a labelled busy region",
          description: t("placeholderPage.test2"),
        },
      ],
    },
  ]}
>
  <Fragment slot="usage">
    <p class="lede" set:html={t("placeholderPage.lede")} />

    <div class="sk-callout" data-tone="info">
      <div class="sk-callout__content">
        <div class="sk-callout__description" set:html={t("placeholderPage.calloutBody")} />
      </div>
    </div>

    <h3>{t("placeholderPage.layoutTitle")}</h3>
    <p set:html={t("placeholderPage.layoutBody")} />
    <ComponentPreview tree={publicationPlaceholderTree(t)} label={t("placeholderPage.layoutLabel")} note={t("placeholderPage.layoutNote")} />

    <h3>{t("placeholderPage.swapTitle")}</h3>
    <p set:html={t("placeholderPage.swapBody")} />
    <ComponentPreview
      tree={publicationSwapTree(t)}
      js={swapScript}
      previewScript={swapScript}
      label={t("placeholderPage.swapLabel")}
      note={t("placeholderPage.swapNote")}
    />

    <h3>{t("placeholderPage.shapesTitle")}</h3>
    <p set:html={t("placeholderPage.shapesBody")} />
    <ul>
      <li set:html={t("placeholderPage.shapesItem1")} />
      <li set:html={t("placeholderPage.shapesItem2")} />
      <li set:html={t("placeholderPage.shapesItem3")} />
      <li set:html={t("placeholderPage.shapesItem4")} />
    </ul>
    <ComponentPreview tree={placeholderVocabularyTree()} label={t("placeholderPage.shapesLabel")} />

    <h3>{t("placeholderPage.matchTitle")}</h3>
    <p set:html={t("placeholderPage.matchBody")} />
    <ComponentPreview
      tree={placeholderMatchTree(t)}
      label={t("placeholderPage.matchLabel")}
      note={t("placeholderPage.matchNote")}
    />

    <h3>{t("placeholderPage.reducedTitle")}</h3>
    <p set:html={t("placeholderPage.reducedBody")} />

    <p set:html={t("placeholderPage.reactBody")} />
  </Fragment>

  <Fragment slot="install">
    <CodeBlock code={install} label="terminal" />
    <CodeBlock code={styles} label="CSS" lang="css" />
  </Fragment>
</ComponentPageShell>
