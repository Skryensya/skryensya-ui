# What changed in table, in the words its users read. Only appended to, never rewritten.
#
# `surface` is the shape of the contract the last time somebody reviewed it. If the compiler says it
# does not match, something a consumer depends on has moved: write the entry and paste in the new
# hash.
surface: "cf6a3e82cfca9eb8"
entries:
  - date: 2026-08-25
    kind: bugfix
    target: Table
    es:
      title: El aviso de resizeLabel dejaba de estar acotado a Table
      body: >-
        La regla de a11y de <code>resizeLabel</code> no traía <code>signatures</code>, así que
        aplicaba a las nueve signatures de la familia por igual. <code>resizableColumns</code> es
        una opción sólo de <code>Table</code>: para <code>TableCaption</code>,
        <code>TableRow</code>, <code>TableHeader</code> y el resto, la condición
        <code>when: { resizableColumns: true }</code> no se podía evaluar (ninguna la declara
        entre sus propias opciones), así que la regla caía a la rama "no evaluable" y avisaba
        siempre, aunque la composición nunca hubiera pedido columnas redimensionables. Se agrega
        <code>signatures: ["Table"]</code>: la regla sólo se evalúa donde la opción que la activa
        realmente existe.
    en:
      title: The resizeLabel advisory used to fire outside Table
      body: >-
        The <code>resizeLabel</code> a11y rule carried no <code>signatures</code>, so it applied
        to all nine signatures in the family equally. <code>resizableColumns</code> is
        <code>Table</code>'s own option: for <code>TableCaption</code>, <code>TableRow</code>,
        <code>TableHeader</code> and the rest, the <code>when: { resizableColumns: true }</code>
        condition could not be evaluated (none of them declare it among their own options), so the
        rule fell to the "unevaluable" branch and warned unconditionally, even when the
        composition never asked for resizable columns at all. Adds
        <code>signatures: ["Table"]</code>: the rule now only evaluates where the option that
        turns it on actually exists.
  - date: 2026-08-16
    kind: feature
    target: Table
    es:
      title: Nueva opción columnWeights, reparto inicial por peso en vez de parejo
      body: >-
        Se agrega <code>columnWeights</code>: un número positivo por columna, separado por comas
        (<code>"2,1,1,1"</code>), que decide el reparto INICIAL del ancho de la tabla antes de
        cualquier arrastre: sólo tiene efecto junto a <code>resizableColumns</code>. Sin ella,
        cada columna arranca igual, el comportamiento de siempre; con pesos iguales el reparto es
        exactamente el mismo. El arrastre en sí no cambia: <code>resolveColumnResize</code> sigue
        redistribuyendo sólo el par de columnas tocado, el peso únicamente decide la semilla.
    en:
      title: New columnWeights option, a weighted initial split instead of an even one
      body: >-
        Adds <code>columnWeights</code>: one positive number per column, comma-separated
        (<code>"2,1,1,1"</code>), that decides the table's INITIAL width split before any drag -
        only meaningful alongside <code>resizableColumns</code>. Omitted, every column starts
        equal, the prior behaviour; equal weights reduce to the exact same split. Dragging itself
        is unchanged: <code>resolveColumnResize</code> still only redistributes the touched pair,
        the weight only decides the seed.
  - date: 2026-08-16
    kind: feature
    target: Table
    es:
      title: Nueva opción resizableColumns, columnas redimensionables por arrastre
      body: >-
        Se agrega <code>resizableColumns</code> (opt-in, apagada por defecto). Encendida, el
        binding inserta un separador WAI-ARIA APG "Window Splitter" real
        (<code>role="separator"</code>, <code>aria-orientation="vertical"</code>,
        flechas/Shift+flecha/Home/End/Enter) entre cada par de encabezados de columna. El mismo
        primitivo compartido (<code>@skryensya/core/splitter</code>) que ya usan el separador de
        <code>Sidebar</code> y el redimensionador de columnas de <code>Treegrid</code>. Requiere
        <code>resizeLabel</code> (nueva opción, gate de a11y): el separador es binding-insertado,
        nunca escrito a mano, así que nada más lo nombra para un lector de pantalla. La primera entrada
        de este archivo decía "no tiene enhancer: el markup está completo por sí solo". Eso sigue
        siendo la regla por defecto: el binding Vanilla solo enhancea un <code>&lt;table&gt;</code>
        que trae <code>data-resizable-columns</code>, así que cualquier tabla que no la pida sigue
        sin JavaScript, exactamente como antes.
    en:
      title: New resizableColumns option, drag-to-resize columns
      body: >-
        Adds <code>resizableColumns</code> (opt-in, off by default). Turned on, the binding
        inserts a real WAI-ARIA APG "Window Splitter" separator (<code>role="separator"</code>,
        <code>aria-orientation="vertical"</code>, arrows/Shift+arrow/Home/End/Enter) between every
        pair of column headers. The same shared primitive
        (<code>@skryensya/core/splitter</code>) Sidebar's own separator and Treegrid's own column
        resizer already use. Requires <code>resizeLabel</code> (a new option, an a11y gate): the
        separator is binding-inserted, never authored, so nothing else names it for a screen
        reader. This file's first entry said "no enhancer: the markup is complete on its own" -
        that stays the default: the vanilla binding only ever enhances a
        <code>&lt;table&gt;</code> that carries <code>data-resizable-columns</code>, so any table
        that never asks for it stays exactly as JavaScript-free as before.
  - date: 2026-08-11
    kind: feature
    es:
      title: Table se publica como contrato, con 9 signatures
      body: >-
        Entra al catálogo con <code>TableScroll</code>, <code>Table</code>, <code>TableCaption</code>,
        <code>TableHead</code>, <code>TableBody</code>, <code>TableFooter</code>, <code>TableRow</code>,
        <code>TableHeader</code>, <code>TableCell</code>: los dos bindings lo realizan, y la referencia
        de esta página sale del contrato compilado en vez de prosa escrita al lado. No tiene enhancer:
        el markup está completo por sí solo, así que las dos bindings son el mismo markup dos veces.
        Opciones: <code>scope</code>, <code>stickyColumn</code>, <code>stickyHeader</code>,
        <code>density</code>, <code>densityFactor</code>, <code>colspan</code>. La hoja es
        <code>components/table.css</code>.
    en:
      title: Table is published as a contract, with 9 signatures
      body: >-
        It joins the catalogue with <code>TableScroll</code>, <code>Table</code>,
        <code>TableCaption</code>, <code>TableHead</code>, <code>TableBody</code>,
        <code>TableFooter</code>, <code>TableRow</code>, <code>TableHeader</code>,
        <code>TableCell</code>: both bindings realize it, and this page's reference is rendered from the
        compiled contract rather than from prose written beside it. There is no enhancer: the markup is
        complete on its own, so both bindings are the same markup twice. Options: <code>scope</code>,
        <code>stickyColumn</code>, <code>stickyHeader</code>, <code>density</code>,
        <code>densityFactor</code>, <code>colspan</code>. Its stylesheet is
        <code>components/table.css</code>.
  - date: 2026-09-12
    kind: feature
    es:
      title: Los styling hooks de table quedan declarados en el contrato
      body: >-
        El contrato ahora publica los <code>25</code> styling hooks de este componente. Nada
        de lo que hayas escrito cambia: son los mismos hooks, con los mismos nombres, y la hoja de
        estilos no se tocó. Lo nuevo es que el contrato los declara, así que el compilador falla si
        la hoja declara uno que el contrato no publica, o si el contrato promete uno que la hoja no
        declara. Esa segunda mitad es la que importa: hasta ahora, redeclarar un hook que ya no
        existía no daba error, daba silencio.
    en:
      title: table's styling hooks are now declared in the contract
      body: >-
        The contract now publishes this component's <code>25</code> styling hooks. Nothing you
        wrote changes: the same hooks, under the same names, and the stylesheet was not touched.
        What is new is that the contract declares them, so the compiler fails when the stylesheet
        declares one the contract does not publish, or the contract promises one the stylesheet
        never declares. That second half is the one that matters: until now, re-declaring a hook
        that no longer existed was not an error, it was silence.
  - date: 2026-09-17
    kind: bugfix
    es:
      title: columnWeights se valida como lista, una entrada por columna
      body: >-
        <code>columnWeights</code> declara que es una lista de números positivos separados por coma
        y cuántas entradas debe tener: una por columna de la primera fila. Antes una lista con un 0 o
        con una entrada de más validaba, y el binding la descartaba en silencio para usar su reparto
        por defecto.
    en:
      title: columnWeights is validated as a list, one entry per column
      body: >-
        <code>columnWeights</code> declares that it is a comma-separated list of positive numbers and
        how many entries it takes: one per column of the first row. A list with a 0 or one entry too
        many used to validate, and the binding silently dropped it for its default split.
  - date: 2026-09-17
    kind: bugfix
    es:
      title: El contrato declara el rango de sus números
      body: >-
        <code>colspan</code> es un entero de 1 a 1000, lo que HTML acepta. Un valor fuera de rango deja de validar en vez de llegar al binding.
    en:
      title: The contract declares the range of its numbers
      body: >-
        <code>colspan</code> is a whole number from 1 to 1000, what HTML accepts. A value out of range stops validating instead of reaching the binding.
  - date: 2026-09-17
    kind: feature
    es:
      title: Los encabezados y las celdas abarcan filas y columnas
      body: >-
        <code>TableHeader</code> acepta <code>colspan</code> y <code>rowspan</code>, y
        <code>TableCell</code> suma <code>rowspan</code>: un encabezado agrupado sobre sus
        subcolumnas no se podía escribir desde un árbol. <code>density</code> y
        <code>densityFactor</code> no bajan de 0.
    en:
      title: Headers and cells span rows and columns
      body: >-
        <code>TableHeader</code> takes <code>colspan</code> and <code>rowspan</code>, and
        <code>TableCell</code> gains <code>rowspan</code>: a grouped header over its subcolumns could
        not be written from a tree. <code>density</code> and <code>densityFactor</code> do not go
        below 0.
  - date: 2026-09-17
    kind: breaking
    es:
      title: Una tabla necesita nombre
      body: >-
        Un <code>Table</code> sin <code>TableCaption</code>, <code>aria-label</code> ni <code>aria-labelledby</code> deja de validar: se anunciaba sólo como tabla.
    en:
      title: A table needs a name
      body: >-
        A <code>Table</code> with no <code>TableCaption</code>, <code>aria-label</code> or <code>aria-labelledby</code> stops validating: it was announced as just a table.
