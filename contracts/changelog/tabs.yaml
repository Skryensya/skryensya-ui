# What changed in tabs, in the words its users read. Only appended to, never rewritten.
#
# `surface` is the shape of the contract the last time somebody reviewed it. If the compiler says it
# does not match, something a consumer depends on has moved: write the entry and paste in the new
# hash.
surface: "16c02346cf5f1dd4"
entries:
  - date: 2026-09-17
    kind: feature
    es:
      title: Firmas interactivas declaran forward
      body: >-
        Las firmas host interactivas o de formulario publican <code>forward</code>
        (<code>id</code>/<code>aria-*</code> y attrs de enlace o formulario cuando aplica).
        Un attr fuera de la lista es <code>unknown-attr</code>.
    en:
      title: Interactive signatures declare forward
      body: >-
        Interactive or form host signatures publish <code>forward</code>
        (<code>id</code>/<code>aria-*</code> and link or form attrs where relevant).
        An attr outside the list is <code>unknown-attr</code>.
  - date: 2026-09-17
    kind: feature
    es:
      title: eventDetails nombra source y trigger
      body: >-
        <code>eventDetails</code> declara <code>source</code> (parte que despacha el CustomEvent) y, cuando es claro, <code>trigger</code> (parte que el usuario activa). Un agente sabe dónde escuchar y qué control provoca el evento.
    en:
      title: eventDetails names source and trigger
      body: >-
        <code>eventDetails</code> declares <code>source</code> (part that dispatches the CustomEvent) and, when clear, <code>trigger</code> (part the user activates). An agent knows where to listen and which control causes the event.
  - date: 2026-09-17
    kind: chore
    es:
      title: El hash de superficie mide la promesa pública
      body: >-
        El hash de superficie ahora proyecta opciones por lista blanca (sin <code>machineInput</code>), incluye <code>eventDetails</code>, y sigue excluyendo la plantilla (<code>also</code>, attrs efímeros). Este contrato no cambió su API; sólo cambió lo que el hash mide.
    en:
      title: Surface hash measures the public promise
      body: >-
        The surface hash now projects options through a whitelist (no <code>machineInput</code>), includes <code>eventDetails</code>, and still excludes the template (<code>also</code>, ephemeral attrs). This contract's API did not change; only what the hash measures did.
  - date: 2026-09-14
    kind: feature
    target: variant
    es:
      title: Tabs suma la opción variant
      body: >-
        <code>variant="hanging"</code> pone la línea arriba de la lista: los triggers cuelgan de
        ella con las esquinas redondeadas abajo, y la barra del seleccionado queda en el borde
        superior. <code>underline</code> sigue siendo el valor por defecto. Es sólo CSS, ortogonal a
        <code>size</code> y <code>orientation</code>.
    en:
      title: Tabs gains the variant option
      body: >-
        <code>variant="hanging"</code> moves the line to the top of the list: the triggers hang from
        it with their rounded corners at the bottom, and the selected bar sits on the top edge.
        <code>underline</code> stays the default. It is CSS-only, orthogonal to <code>size</code>
        and <code>orientation</code>.
  - date: 2026-08-20
    kind: feature
    target: size
    es:
      title: Tabs suma la opción size
      body: >-
        <code>size</code> controla el alto de la fila de triggers, su relleno horizontal y el
        tamaño de la etiqueta: <code>sm</code> o <code>md</code> (por defecto). Es sólo CSS,
        ortogonal a <code>orientation</code>: cualquier orientación puede ser <code>sm</code>.
    en:
      title: Tabs gains the size option
      body: >-
        <code>size</code> controls the trigger row's height, its inline padding and the label
        size: <code>sm</code> or <code>md</code> (default). It is CSS-only, orthogonal to
        <code>orientation</code>: any orientation can be <code>sm</code>.
  - date: 2026-08-11
    kind: feature
    es:
      title: Tabs se publica como contrato, con una signature
      body: >-
        Entra al catálogo con <code>Tabs</code>: los dos bindings lo realizan, y la referencia de esta
        página sale del contrato compilado en vez de prosa escrita al lado. No tiene enhancer: el markup
        está completo por sí solo, así que las dos bindings son el mismo markup dos veces. Opciones:
        <code>orientation</code>, <code>activationMode</code>, <code>value</code>. La hoja es
        <code>components/tabs.css</code>.
    en:
      title: Tabs is published as a contract, with one signature
      body: >-
        It joins the catalogue with <code>Tabs</code>: both bindings realize it, and this page's
        reference is rendered from the compiled contract rather than from prose written beside it. There
        is no enhancer: the markup is complete on its own, so both bindings are the same markup twice.
        Options: <code>orientation</code>, <code>activationMode</code>, <code>value</code>. Its
        stylesheet is <code>components/tabs.css</code>.
  - date: 2026-09-12
    kind: feature
    es:
      title: Los styling hooks de tabs quedan declarados en el contrato
      body: >-
        El contrato ahora publica los <code>15</code> styling hooks de este componente. Nada
        de lo que hayas escrito cambia: son los mismos hooks, con los mismos nombres, y la hoja de
        estilos no se tocó. Lo nuevo es que el contrato los declara, así que el compilador falla si
        la hoja declara uno que el contrato no publica, o si el contrato promete uno que la hoja no
        declara. Esa segunda mitad es la que importa: hasta ahora, redeclarar un hook que ya no
        existía no daba error, daba silencio.
    en:
      title: tabs's styling hooks are now declared in the contract
      body: >-
        The contract now publishes this component's <code>15</code> styling hooks. Nothing you
        wrote changes: the same hooks, under the same names, and the stylesheet was not touched.
        What is new is that the contract declares them, so the compiler fails when the stylesheet
        declares one the contract does not publish, or the contract promises one the stylesheet
        never declares. That second half is the one that matters: until now, re-declaring a hook
        that no longer existed was not an error, it was silence.
  - date: 2026-09-16
    kind: bugfix
    es:
      title: El validador comprueba que el valor inicial nombre una entrada que existe
      body: >-
        <code>value</code> ahora declara que nombra entradas de <code>items</code> por su
        <code>value</code>. Un árbol que apunta a una entrada inexistente deja de validar
        (<code>unknown-key</code>) en vez de renderizar sin nada seleccionado ni decir por qué. El
        markup y los bindings no cambian.
    en:
      title: The validator checks that the initial value names an entry that exist
      body: >-
        <code>value</code> now declares that it names entries of <code>items</code> by their
        <code>value</code>. A tree pointing at an entry that does not exist stops validating
        (<code>unknown-key</code>) instead of rendering with nothing selected and no reason given.
        Markup and bindings are unchanged.
  - date: 2026-09-17
    kind: breaking
    es:
      title: Cada evento lleva el nombre de su familia
      body: >-
        <code>sk-value-change</code> → <code>sk:tabsvaluechange</code>. Un mismo nombre ya no lo despachan varias familias: un listener sobre una raíz deja
        de recibir los eventos de un componente distinto anidado adentro. El contrato ahora los declara en <code>events</code>, así que el compilador los publica.
    en:
      title: Every event carries its family in its name
      body: >-
        <code>sk-value-change</code> → <code>sk:tabsvaluechange</code>. No two families dispatch the same name any more: a listener on a root stops receiving
        the events of a different component nested inside it. The contract now declares them in <code>events</code>, so the compiler publishes them.
  - date: 2026-09-17
    kind: bugfix
    es:
      title: Toda lista de pestañas necesita un nombre
      body: >-
        La regla que pide <code>aria-label</code> o <code>aria-labelledby</code> sólo se activaba si
        el árbol escribía <code>orientation</code>, que casi nunca se escribe porque tiene default.
        Ahora aplica siempre.
    en:
      title: Every tab list needs a name
      body: >-
        The rule asking for <code>aria-label</code> or <code>aria-labelledby</code> only fired when the
        tree wrote <code>orientation</code>, which almost nobody writes because it has a default. It
        now always applies.
