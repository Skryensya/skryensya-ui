# What changed in typography, in the words its users read. Only appended to, never rewritten.
#
# `surface` is the shape of the contract the last time somebody reviewed it. If the compiler says it
# does not match, something a consumer depends on has moved: write the entry and paste in the new
# hash.
surface: "f91549c36afb6b75"
entries:
  - date: 2026-09-17
    kind: feature
    es:
      title: Firmas interactivas declaran forward
      body: >-
        Las firmas host interactivas o de formulario publican <code>forward</code>
        (<code>id</code>/<code>aria-*</code> y attrs de enlace o formulario cuando aplica).
        Un attr fuera de la lista es <code>unknown-attr</code>.
    en:
      title: Interactive signatures declare forward
      body: >-
        Interactive or form host signatures publish <code>forward</code>
        (<code>id</code>/<code>aria-*</code> and link or form attrs where relevant).
        An attr outside the list is <code>unknown-attr</code>.
  - date: 2026-09-17
    kind: bugfix
    target: Code
    es:
      title: React tipa Code children como texto
      body: >-
        <code>Code</code> declara <code>children: string</code>, igual que el slot del contrato
        (<code>accepts: "text"</code>). Un bloque resaltado sigue siendo <code>code-preview</code>.
    en:
      title: React types Code children as text
      body: >-
        <code>Code</code> declares <code>children: string</code>, matching the contract slot
        (<code>accepts: "text"</code>). A highlighted block remains <code>code-preview</code>.
  - date: 2026-08-23
    kind: breaking
    target: linkTone
    es:
      title: linkTone "primary" pasa a llamarse "accent"
      body: >-
        Mismo cambio que <code>button</code> (misma fecha, mismo motivo): <code>linkTone</code>
        solo tenía un valor posible, <code>primary</code>, y ese nombre describía el color como
        una posición de jerarquía ("el tono principal") cuando en realidad es un interruptor
        binario. El link toma el color de la marca o no lo toma, sin escala entre medio. La
        cadena de tokens detrás (<code>--color-text-link</code> → <code>--color-text-accent</code>)
        ya decía "accent" dos niveles abajo; el nombre de la opción ahora dice lo mismo. Distinto
        de <code>Text</code>'s <code>tone</code> (<code>primary</code>/<code>secondary</code>/
        <code>tertiary</code>/<code>danger</code>), que no se toca: ahí "primary" es el primer
        peldaño de una escala de énfasis, no una afirmación de intención. Un consumidor con
        <code>data-tone="primary"</code> en un <code>&lt;a&gt;</code> escrito a mano deja de
        pintar el color de marca; <code>linkTone="primary"</code> deja de tipar.
    en:
      title: linkTone "primary" is renamed to "accent"
      body: >-
        Same change as <code>button</code> (same date, same reason): <code>linkTone</code> only
        ever had one possible value, <code>primary</code>, and that name described the color as a
        rank in a hierarchy ("the main tone") when it is really a binary switch. The link either
        takes the brand color or it does not, no scale in between. The token chain behind it
        (<code>--color-text-link</code> → <code>--color-text-accent</code>) already said "accent"
        two levels down; the option's name now says the same thing. Different from
        <code>Text</code>'s <code>tone</code> (<code>primary</code>/<code>secondary</code>/
        <code>tertiary</code>/<code>danger</code>), which is untouched: there "primary" is the
        first rung of an emphasis scale, not a claim about intent. A consumer with hand-authored
        <code>data-tone="primary"</code> on an <code>&lt;a&gt;</code> stops painting the brand
        color; <code>linkTone="primary"</code> stops type-checking.
  - date: 2026-08-11
    kind: feature
    es:
      title: Typography se publica como contrato, con 6 signatures
      body: >-
        Entra al catálogo con <code>Text</code>, <code>Strong</code>, <code>Output</code>,
        <code>Code</code>, <code>Heading</code>, <code>Link</code>: los dos bindings lo realizan, y la
        referencia de esta página sale del contrato compilado en vez de prosa escrita al lado. No tiene
        enhancer: el markup está completo por sí solo, así que las dos bindings son el mismo markup dos
        veces. Opciones: <code>tone</code>, <code>size</code>, <code>weight</code>,
        <code>headingSize</code>, <code>flush</code>, <code>href</code>, <code>linkTone</code>. La hoja
        es <code>components/typography.css</code>.
    en:
      title: Typography is published as a contract, with 6 signatures
      body: >-
        It joins the catalogue with <code>Text</code>, <code>Strong</code>, <code>Output</code>,
        <code>Code</code>, <code>Heading</code>, <code>Link</code>: both bindings realize it, and this
        page's reference is rendered from the compiled contract rather than from prose written beside
        it. There is no enhancer: the markup is complete on its own, so both bindings are the same
        markup twice. Options: <code>tone</code>, <code>size</code>, <code>weight</code>,
        <code>headingSize</code>, <code>flush</code>, <code>href</code>, <code>linkTone</code>. Its
        stylesheet is <code>components/typography.css</code>.
  - date: 2026-09-12
    kind: feature
    es:
      title: Los styling hooks de typography quedan declarados en el contrato
      body: >-
        El contrato ahora publica los <code>9</code> styling hooks de este componente. Nada
        de lo que hayas escrito cambia: son los mismos hooks, con los mismos nombres, y la hoja de
        estilos no se tocó. Lo nuevo es que el contrato los declara, así que el compilador falla si
        la hoja declara uno que el contrato no publica, o si el contrato promete uno que la hoja no
        declara. Esa segunda mitad es la que importa: hasta ahora, redeclarar un hook que ya no
        existía no daba error, daba silencio.
    en:
      title: typography's styling hooks are now declared in the contract
      body: >-
        The contract now publishes this component's <code>9</code> styling hooks. Nothing you
        wrote changes: the same hooks, under the same names, and the stylesheet was not touched.
        What is new is that the contract declares them, so the compiler fails when the stylesheet
        declares one the contract does not publish, or the contract promises one the stylesheet
        never declares. That second half is the one that matters: until now, re-declaring a hook
        that no longer existed was not an error, it was silence.
  - date: 2026-09-15
    kind: rework
    es:
      title: El kit ahora trae su propia tipografía, Hanken Grotesk
      body: >-
        <code>--scale-font-family-sans</code> nombraba <code>Inter</code> y nada en el kit la cargaba,
        así que toda superficie caía a <code>system-ui</code>: SF en un Mac, Segoe en Windows, Roboto en
        Android, tres ritmos para un solo sistema. Ahora el paquete trae la familia
        (<code>@skryensya/core/fonts/hanken-grotesk.css</code>, un import opcional): eje variable de
        100 a 900, itálica dibujada, subsets latin y latin-ext, y una cara de respaldo con
        <code>local()</code> y las métricas reales medidas encima, para que el intercambio cambie el
        dibujo y no el layout (0.38% de diferencia de ancho a 12px). La elección se midió sobre estas
        mismas páginas; el razonamiento y la tabla están en
        <code>docs/decisions/0023-the-kit-ships-its-own-default-sans.md</code>. Si tu marca usa otra
        familia, sigue siendo un preset: pisás <code>--font-family-body</code> y no importás la hoja.
    en:
      title: The kit now ships its own typeface, Hanken Grotesk
      body: >-
        <code>--scale-font-family-sans</code> named <code>Inter</code> and nothing in the kit ever
        loaded it, so every surface resolved to <code>system-ui</code>: SF on a Mac, Segoe on Windows,
        Roboto on Android, three rhythms for one system. The package now ships the family
        (<code>@skryensya/core/fonts/hanken-grotesk.css</code>, an optional import): a variable 100-900
        weight axis, a drawn italic, latin and latin-ext subsets, and a fallback face built from
        <code>local()</code> sources with the real font's measured metrics on it, so the swap changes
        the drawing and not the layout (0.38% width difference at 12px). The choice was measured on
        these very pages; the reasoning and the table live in
        <code>docs/decisions/0023-the-kit-ships-its-own-default-sans.md</code>. If your brand uses a
        different family, that is still a preset: override <code>--font-family-body</code> and do not
        import the sheet.
  - date: 2026-09-16
    kind: feature
    es:
      title: Text publica sus roles de título y Output nombra sus entradas
      body: >-
        <code>textRole</code> (<code>eyebrow</code>, <code>subtitle</code>) entra al contrato: los
        roles ya estaban en la hoja, pero un árbol sólo llegaba a ellos con un
        <code>data-role</code> que nadie validaba. Output suma <code>outputFor</code>
        (<code>htmlFor</code> en React). En React, <code>Code</code> ahora exige children, como su slot.
    en:
      title: Text publishes its title-block roles and Output names its inputs
      body: >-
        <code>textRole</code> (<code>eyebrow</code>, <code>subtitle</code>) joins the contract: the
        roles were already in the stylesheet, but a tree could only reach them through a
        <code>data-role</code> nothing validated. Output gains <code>outputFor</code>
        (<code>htmlFor</code> in React). In React, <code>Code</code> now requires children, as its slot does.
  - date: 2026-09-16
    kind: feature
    es:
      title: Heading y Text eligen su elemento desde el árbol
      body: >-
        <code>headingElement</code> (<code>h1</code>–<code>h6</code>) y <code>textElement</code>
        (<code>p</code>, <code>div</code>, <code>span</code>) llegan al contrato. Hasta ahora todo
        Heading compuesto desde un árbol salía como <code>h2</code>, así que un título de página no
        podía ser un <code>h1</code>. En React siguen siendo la prop <code>as</code>; el aspecto
        sigue en <code>size</code>.
    en:
      title: Heading and Text choose their element from the tree
      body: >-
        <code>headingElement</code> (<code>h1</code>–<code>h6</code>) and <code>textElement</code>
        (<code>p</code>, <code>div</code>, <code>span</code>) join the contract. Until now every
        Heading composed from a tree came out as an <code>h2</code>, so a page title could not be an
        <code>h1</code>. In React they are still the <code>as</code> prop; appearance stays on
        <code>size</code>.
  - date: 2026-09-17
    kind: bugfix
    es:
      title: textRole excluye size, tone y weight, y los alias de Heading avisan
      body: >-
        Un Text con <code>textRole</code> y además <code>size</code>, <code>tone</code> o <code>weight</code> deja de validar: el rol ya los fija y ganaba en silencio. <code>headingSize</code> <code>sm</code>, <code>md</code>, <code>lg</code> y <code>display</code> siguen siendo válidos, pero el validador sugiere <code>h3</code>, <code>h2</code>, <code>h1</code> y <code>display-sm</code>.
    en:
      title: textRole excludes size, tone and weight, and Heading's aliases advise
      body: >-
        A Text with <code>textRole</code> plus <code>size</code>, <code>tone</code> or <code>weight</code> stops validating: the role already sets them and silently won. <code>headingSize</code> <code>sm</code>, <code>md</code>, <code>lg</code> and <code>display</code> stay valid, but the validator suggests <code>h3</code>, <code>h2</code>, <code>h1</code> and <code>display-sm</code>.
  - date: 2026-09-17
    kind: chore
    es:
      title: El hash de superficie cubre también las restricciones
      body: >-
        El hash de superficie ahora incluye las restricciones entre opciones (<code>implies</code>, <code>excludes</code>, <code>notInside</code>, <code>pairs</code>) y <code>outputHooks</code>. Este contrato no cambió; sólo cambió lo que el hash mide.
    en:
      title: The surface hash also covers constraints
      body: >-
        The surface hash now covers the constraints between options (<code>implies</code>, <code>excludes</code>, <code>notInside</code>, <code>pairs</code>) and <code>outputHooks</code>. This contract did not change; only what the hash measures did.
