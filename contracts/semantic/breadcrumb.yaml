# The structure lives in packages/core/src/breadcrumb.ts. Here, only why it is chosen.

Breadcrumb:
  useWhen:
    - "la página vive dentro de una jerarquía y el usuario necesita volver hacia arriba"
  avoidWhen:
    - "la navegación es plana; eso es un NavList"
    - "son pasos de un proceso; eso es Steps"
  alternatives:
    - NavList
