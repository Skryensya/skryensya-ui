# The structure lives in packages/core/src/progress.ts. Here, only why it is chosen.

Progress:
  useWhen:
    - "se conoce cuánto falta: una subida, un formulario por pasos, una descarga"
  avoidWhen:
    - "la espera es de duración desconocida; eso es un Loader"
  alternatives:
    - Loader
