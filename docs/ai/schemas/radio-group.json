{
  "$schema": "./_meta.json",
  "id": "radio-group",
  "consume": {
    "css": "@skryensya/core/components/radio-group.css",
    "prefer": "react"
  },
  "surfaces": {
    "RadioGroup": {
      "use": "exclusive selection",
      "element": "div",
      "react": {
        "from": "@skryensya/react/radio-group",
        "name": "RadioGroup"
      },
      "requires": ["items", "name"],
      "props": {
        "orientation": ["vertical", "horizontal"]
      }
    }
  },
  "rules": {
    "items": "Give every item a stable unique value and label.",
    "accessibleName": "RadioGroup has no visible label of its own; provide aria-label, aria-labelledby, or wrap it in a fieldset with a legend."
  },
  "examples": {
    "RadioGroup": {
      "vertical": "<RadioGroup aria-label=\"Plan\" defaultValue=\"pro\" items={plans} name=\"plan\" />",
      "horizontal": "<RadioGroup aria-label=\"Shipping\" items={shipping} name=\"shipping\" orientation=\"horizontal\" value={shippingValue} onValueChange={setShipping} />"
    }
  }
}
