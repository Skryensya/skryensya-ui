/*
 * The clipboard write, kept at its old import path. It lives in `clipboard.ts` now, beside the
 * `clipboard` contract (CopyButton, Clipboard) that decision 33 had withdrawn and this reverses.
 */
export { writeClipboard } from "./clipboard.js";
