{
  "name": "@skryensya/devtools",
  "version": "0.0.1-dev",
  "private": true,
  "type": "module",
  "description": "Optional runtime debug overlay for skryensya/ui: hit-area visualization and other inspection aids, built from the design system's own components. No package in this repo imports it, so it costs nothing unless a consumer opts in.",
  "files": [
    "src"
  ],
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    },
    "./checks": {
      "types": "./src/checks.ts",
      "default": "./src/checks.ts"
    }
  },
  "scripts": {
    "check": "vitest run && tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@skryensya/core": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "vitest": "^4.1.11"
  }
}
