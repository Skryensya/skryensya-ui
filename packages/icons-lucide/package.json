{
  "name": "@skryensya/icons-lucide",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Lucide bound to skryensya/ui's stable icon vocabulary. Generated from the library; ships data, not a runtime.",
  "files": ["dist", "src", "LICENSE-lucide"],
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    },
    "./carousel.css": "./src/carousel.css"
  },
  "scripts": {
    "generate": "node scripts/generate.ts",
    "build": "tsc -p tsconfig.json",
    "check": "node scripts/generate.ts --check && tsc -p tsconfig.json --noEmit && tsc -p tsconfig.scripts.json",
    "lint": "node scripts/generate.ts --check && tsc -p tsconfig.json --noEmit && tsc -p tsconfig.scripts.json"
  },
  "dependencies": {
    "@skryensya/core": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "lucide": "^1.24.0",
    "typescript": "^5.9.3"
  }
}
