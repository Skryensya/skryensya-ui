{
  "name": "@skryensya/icons-material",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Material Symbols bound to skryensya/ui's stable icon vocabulary. Generated from the library; ships data, not a runtime.",
  "files": ["dist", "src", "LICENSE-material"],
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    },
    "./carousel.css": "./src/carousel.css"
  },
  "scripts": {
    "generate": "node scripts/generate.mjs",
    "build": "tsc -p tsconfig.json",
    "check": "node scripts/generate.mjs --check && tsc -p tsconfig.json --noEmit",
    "lint": "node scripts/generate.mjs --check && tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@skryensya/core": "workspace:*"
  },
  "devDependencies": {
    "@material-symbols/svg-400": "^0.45.8",
    "typescript": "^5.9.3"
  }
}
