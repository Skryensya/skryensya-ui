{
  "name": "@skryensya/mcp",
  "version": "0.0.1-dev",
  "private": true,
  "type": "module",
  "description": "MCP server over the compiled manifest: get_catalog, get_contract and validate_ui. Rebuilt from zero; the v1 server that read docs/ai/schemas is gone (tag ai-v1).",
  "scripts": {
    "build": "node scripts/build.ts",
    "start": "node dist/index.js",
    "start:http": "node dist/http.js",
    "check": "node scripts/build.ts && tsc -p tsconfig.json --noEmit && tsc -p tsconfig.scripts.json && vitest run",
    "inspector": "npx @modelcontextprotocol/inspector node dist/index.js"
  },
  "bin": {
    "skryensya-mcp": "./dist/index.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.30.0",
    "@skryensya/ai-compiler": "workspace:*",
    "@skryensya/core": "workspace:*",
    "@skryensya/snippets": "workspace:*",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "tsx": "^4.19.2",
    "typescript": "^5.9.3",
    "vitest": "^4.1.11",
    "esbuild": "^0.28.1"
  }
}
